version: "3.8"

services:
  mysqldb:
    image: mysql:8
    container_name: mysql-taskdb
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: taskdb
    ports:
      - "3311:3306"
    volumes:
      - mysql_data:/var/lib/mysql
    networks:
      - springboot-mysql-net
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-uroot", "-pdocker"]
      interval: 10s
      timeout: 5s
      retries: 5

  backend:
    image: priyankasml/task-backend:latest
    container_name: springboot-taskdb
    depends_on:
      mysqldb:
        condition: service_healthy
    ports:
      - "8081:2000"
    restart: on-failure
    networks:
      - springboot-mysql-net

  frontend:
    image: priyankasml/task-frontend:latest
    container_name: react-taskdb
    depends_on:
      - backend
    ports:
      - "3020:80"
    restart: on-failure
    networks:
      - springboot-mysql-net

volumes:
  mysql_data:

networks:
  springboot-mysql-net: